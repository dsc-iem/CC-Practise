#include<bits/stdc++.h>
using namespace std;

int main(){
    int n,d;
    cin >> n >> d;
    vector<int>a;
    for(int i=0;i<n;i++){
            int x;
            cin >> x;
            a.push_back(x);
    }
    for(int i=d;i<n;i++){
            cout << a[i] << " ";
    }
    for(int i=0;i<d;i++){
            cout << a[i] << " ";
    }

    return 0;
}
